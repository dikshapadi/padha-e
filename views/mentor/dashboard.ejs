<!DOCTYPE html>
<html>
<head>
    <title>Mentor Dashboard</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.1/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-4bw+/aepP/YC94hEpVNVgiZdgIC5+VKNBQNGCHeKRQN+PtmoHDEXuppvnDJzQIu9" crossorigin="anonymous">
    <style>
        /* Your CSS styles here */
        .container {
            max-width: 800px; /* Adjust the value as needed */
            margin: 0 auto; /* Center the container horizontally */
            padding: 20px; /* Add some padding for spacing */
        }

        /* Center-align text and image */
        body {
            background-color: #f0f0f0; /* Off-white background color */
            text-align: center;
            position: relative;
        }

        .center-text {
            text-align: center;
        }

        .stages p {
            text-align: left;
            overflow: hidden;
            margin-left: 50px;
        }

        .stages span.yellow {
            color: orangered;
        }

        .stages span.orange {
            color: orangered;
        }

        .stages span.orange-red {
            color: orangered;
        }

        .stages span.red {
            color: orangered;
        }

        .stages {
            max-width: 100%; /* Adjust as needed */
        }

        .text {
            font-weight: normal; /* Make it normal (not bold) */
            color: #575454; /* Set the color to a light gray or your preferred light color */
        }

        /* Style for the "mentor" text */
        .text-bold-dark {
            font-weight: bold; /* Make it bold */
            color: #000; /* Set the color to dark (black) */
        }

        /* Style for the whole header if needed */
       

        img {
            display: block;
            margin: 0 auto; /* Center horizontally */
        }

        /* Style the buttons */
        button {
            padding: 15px 30px; /* Increase button size */
            margin: 65px 65px 65px 65px; /* Add space between buttons */
            font-size: 18px; /* Increase font size */
            background-color: #000080; /* Dark blue background color */
            color: #fff; /* White text color */
            border: none; /* Remove button border */
        }

        #menteeList {
            text-align: center; /* Center-align "Mentee List" */
        }

        /* Style for the "No mentees to display" text */
        #menteeListItems {
            text-align: center; /* Center-align the text */
            font-size: 24px; /* Increase font size */
            color: red; /* Make the text red */
        }

        /* Style for the Google Translate element */
        #google_translate_element {
            position: absolute;
            top: 20px; /* Adjust the top position to your preference */
            right: 20px; /* Adjust the right position to your preference */
        }
    </style>
</head>
<body>
<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.1/dist/js/bootstrap.bundle.min.js" integrity="sha384-HwwvtgBNo3bZJJLYd8oVXjrBZt8cqVSpeBNS5n7C8IVInixGAoxmnlMuBnhbgrkm" crossorigin="anonymous"></script>
<div id="google_translate_element"></div>
<br><br>
<h1 class="welcome-text">
    <span class="text">Welcome back</span> <span class="text-bold-dark">mentor</span>!
</h1>

<br><br>
<!-- Add your image source and alt text as needed -->
<img src="mentor_main.jpg" alt="Mentor Image" width="80%">
<!-- Buttons with increased size and spacing -->
<button id="showMenteeListBtn">Show Mentee List</button>
<button id="uploadCourseBtn">Upload Course</button>
<button id="goToRDSectionBtn">Go to R&D Section</button>
<button id="inviteMenteeForRDBtn">Invite a Mentee for R&D</button>

<!-- Empty mentee list initially hidden -->
<div id="menteeList" style="display: none;">
    <h2>Mentee List</h2>
    <ul id="menteeListItems">
        <!-- Empty list to be populated dynamically -->
    </ul>
</div>

<!-- R&D Section content initially hidden -->
<div id="rdSection" style="display: none;">
    <h2>R&D Section</h2>
    <!-- Add your R&D section content here -->
</div>

<!-- Input element for file upload (hidden by default) -->
<input type="file" id="fileInput" style="display: none;" multiple>

<br><br><br><br>
<!-- Your provided text with blue-colored and bold headings -->

<div class="row">
    <div class="row center-text"> <!-- Center the text within the row -->
        <div class="col-sm-12">
            <h3 style="color: darkblue;">FRAMEWORK OF MENTORSHIP - 4 STAGES AND UNDERLYING SKILLS</h3>
            <br>
            <p><b>Mentoring centers on the creation of a safe and healthy relationship between a mentor and mentee.&nbsp;Our mentoring framework identifies 4 critical stages every mentor and mentee goes through:</b></p>
        </div>
        <br><br><br><br><br>
    </div>
    <div class="row">
        <div class="col-sm-6">
            <div class="stages">
                <p><span class="yellow">CONNECT: </span>Our first relationship stage helps mentor and the mentee establish trust, consistency and reliability in the relationship.By the end of this stage, mentors and mentees feel comfortable around each other.</p>
            </div>
        </div>
        <div class="col-sm-6">
            <div class="stages">
                <p><span class="orange">EMPOWER: </span>In this stage, mentees work on those facets of their lives that they would like to empower through the mentoring. Mentors and mentees practice different life skills that can help them achieve their goals.</p>
            </div>
        </div>
        <br><br><br><br>
        <div class="col-sm-6">
            <div class="stages">
                <p><span class="orange-red">LEVEL: </span>In the second stage, we remove implicit and explicit power differences between our mentors and mentees. We establish a mutual or two-way relationship, where mentor and mentee both contribute to the relationship and learn from it.</p>
            </div>
        </div>
        <div class="col-sm-6">
            <div class="stages">
                <p><span class="red">EVOLVE: </span>In this final stage, mentees work on their future life choices. Mentors help mentees in their decision-making. Mentors and mentees also reflect on their mentorship and decide the next stage of their own journey.</p>
            </div>
        </div>
    </div>
</div>

<script src="//translate.google.com/translate_a/element.js?cb=googleTranslateElementInit"></script>
<!-- JavaScript to handle button clicks -->
<script>
    function googleTranslateElementInit(){
        new google.translate.TranslateElement(
            {pageLanguage:'en'},
            'google_translate_element'
        );
    }

    // Show the mentee list when the button is clicked
    document.getElementById("showMenteeListBtn").addEventListener("click", function () {
        // Get the mentee list container
        const menteeList = document.getElementById("menteeList");

        // Show the mentee list
        menteeList.style.display = "block";

        // Get the list element
        const menteeListItems = document.getElementById("menteeListItems");

        // Clear any existing list items
        menteeListItems.innerHTML = "";

        // Add an empty list item (you can populate this dynamically later)
        const listItem = document.createElement("li");
        listItem.textContent = "No mentees to display";
        menteeListItems.appendChild(listItem);
    });

    // Show the R&D section when the button is clicked
    document.getElementById("goToRDSectionBtn").addEventListener("click", function () {
        // Get the R&D section container
        const rdSection = document.getElementById("rdSection");

        // Show the R&D section
        rdSection.style.display = "block";
    });

    // Handle the "Upload Course" button click
    document.getElementById("uploadCourseBtn").addEventListener("click", function () {
        // Trigger the file input dialog
        const fileInput = document.getElementById("fileInput");
        fileInput.click();

        // Listen for changes in the file input and handle the selected files
        fileInput.addEventListener("change", function () {
            const selectedFiles = fileInput.files; // Get the selected files

            if (selectedFiles.length > 0) {
                // You can now process the selected files, for example, upload them to your server
                // Here, we're just displaying a message with the file names
                let fileNames = "";
                for (let i = 0; i < selectedFiles.length; i++) {
                    fileNames += selectedFiles[i].name + ", ";
                }
                fileNames = fileNames.slice(0, -2); // Remove the trailing comma and space
                alert(`You selected the files: ${fileNames}`);
            } else {
                alert("No files selected");
            }
        });
    });

    // Handle the "Invite a Mentee for R&D" button click (Add your logic here)
    document.getElementById("inviteMenteeForRDBtn").addEventListener("click", function () {
        // Add your code to invite a mentee for R&D
    });
</script>
</body>
</html>
